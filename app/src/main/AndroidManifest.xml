<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permisos -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" tools:node="remove"/>

    <application

        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Hackaton"
        tools:targetApi="31">

        <!-- Clave de API para Google Maps -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyC3all87KlGUNUiofUeWnYFajhqDYrjK8Q"
            />
        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id"
            tools:replace="android:value"
            />

        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@mipmap/ic_launcher" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/white" />


        <!-- Actividad Principal -->
        <activity android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>



        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Hackaton" />

        <!-- Otras actividades -->
        <activity android:name=".MapaUsuarios" android:exported="true" android:theme="@style/Theme.Hackaton" />
        <activity android:name=".EleccionRegis" android:exported="true" android:theme="@style/Theme.Hackaton" />
        <activity android:name=".UsuarioRegistrar" android:exported="true" android:theme="@style/Theme.Hackaton" />
        <activity android:name=".EmpleadorRegistrar" android:exported="true" android:theme="@style/Theme.Hackaton" />
        <activity android:name=".PortafolioUser" android:exported="true" android:theme="@style/Theme.Hackaton" />

        <!-- Actividades adicionales requeridas -->
        <activity
            android:name="androidx.test.core.app.InstrumentationActivityInvoker$EmptyActivity"
            tools:node="remove" />
        <activity
            android:name="androidx.test.core.app.InstrumentationActivityInvoker$EmptyFloatingActivity"
            tools:node="remove" />


    </application>
</manifest>